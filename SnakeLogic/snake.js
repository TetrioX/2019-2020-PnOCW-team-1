
const Snake = class {

    constructor(startPosition, length, name, speed) {
        this.position = position;
        this.length = length;
        this.name = name;
    }

    const name;

    // Position

    const getPosition = function() {
      return position;
    }

    const setPosition = function(newPos) {
      this.position = newPos;
    }

    var position;

    // Length

    const getLength = function() {
      return position;
    }

    const setLength = function(newLen) {
      this.length = newLen;
    }

    var length;

    // Movement

    const updatePosition = function(direction, spd = speed) {
      pos = getPosition();
      pos.x =   
    }

    var speed;
}
